server {
  listen 80;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri /index.html;  # ✅ if no file found, fallback to React
  }

  error_page 404 /index.html;    # ✅ ensures React handles unknown routes
}
